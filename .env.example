# =============================================================================
# APIOps Environment Configuration
# =============================================================================
#
# INSTRUCCIONES:
#   1. Copiar este archivo: cp .env.example .env
#   2. Reemplazar los valores con tus credenciales reales
#   3. NUNCA commitear .env a Git (ya está en .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# GITHUB RUNNERS CONFIGURATION
# -----------------------------------------------------------------------------
# El entorno local usa 2 runners separados (aislamiento de fallos):
#   - github-runner-exporter: para apim-exporter-wso2
#   - github-runner-controller: para apim-apiops-controller
#
# Ambos usan el mismo token y owner, el repo se configura en docker-compose.yml

# Personal Access Token de GitHub
# Crear en: https://github.com/settings/tokens
# Scopes requeridos: repo, workflow
GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Owner del repositorio (organización o usuario)
GITHUB_OWNER=tu-organizacion
